apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: datadog-dev-as
  namespace: tp-datadog
spec:
  generators:
  - git:
      repoURL: git@github.com:adeo/OBS-HelmDatadog.git
      revision: main
      directories:
      - path: charts/datadog/clusters/clusters_dev/*/*  
  template:
    metadata:
      name: '{{path[4]}}--{{path.basename}}-datadog'
    spec:
      project: datadog
      source:
        repoURL: git@github.com:adeo/OBS-HelmDatadog.git
        targetRevision: main
        path: charts/datadog/datadog-dev
        helm:
          valueFiles:
          - 'values.yaml'
          - '../../../{{path}}/values.yaml'
      destination:
        name: '{{path.basename}}'
        namespace: datadog-agent
      syncPolicy: {}
